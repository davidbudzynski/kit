# https://docs.codecov.com/docs/codecov-yaml
coverage:
  status:
    project:
      default:
        # Issue #63 acceptance: require > 90% overall, with attention to
        # R/ wrappers and src/psum.c fpminR/fpmaxR/prangeR.
        target: 90%
        threshold: 0%
    patch:
      default:
        target: 90%
        threshold: 0%
